2025.2.12
C++ 野指针
1.delete p或free(p)后，p指针所指内存被释放，p指针本身仍存在，扔指向该内存，此时成为野指针
2.函数内定义指针变量在函数结束后会回收内存

2025.2.15
指针存储的变量的内存地址不是自身地址，链表中next指针存储的是另一个结构体变量的地址

1.链表逆序：头插法
2.查找倒数第k个节点：双指针，P指向head,p1指向head+k(注意k是否越界)
3.有序链表合并：定义新的头结点
2025.2.16
链表环问题以及环入口地址：快慢指针判断环，根据（Distance-slow）*2=Distance-fast

2025.2.17
栈：括号匹配问题：左括号进行入栈，遇右括号进行匹配出栈
中缀转后缀：数字不管，栈为空则符号入栈，不为空则符号比较栈内符号优先级，栈内元素优先级大则出栈，小则入栈
后缀表达式计算：遇数字则进栈，遇符号则从栈中取出两个数字计算，随后将数字进栈

2025.2.18
队列：用栈模拟队列:利用两个栈，一个栈负责push一个栈负责进行pop操作，即当pop操作时栈a将数据从a中pop并push进栈b
C++特性：深拷贝和浅拷贝：深拷贝需要手动创建构造函数，默认为浅拷贝，类中地址会复制，浅拷贝可能会导致出错
2025.2.24
P2P模式如何通信，NAT转换隐藏真实ip，udp打洞，借助公网服务器
2025.2.25
指针常量：该指针是一个常量，即初始化后不可指向其他变量
常量指针：该指针指向常量，可以改变指向，但不能通过指针改变指向对象的值
2025.3.1
容器空间配置器，若空间配置器相同，则直接进行移动构造
RAII机制 资源构造初始化,防止内存泄露
hash 算法和位图算法 用于查找多余元素和不存在元素的算法
stl容器中的unordered_set,unordered_map等容器底层采用链式hash表和红黑树，key不能重复

2025 3.2 
bloom filter:位图和hash结合
2025.3.7
命令空间：不同文件命名空间相同，但内部成员变量需要不同
2025.3.8
链表排序算法归并排序：函数形参为参入参数的深拷贝，类似于重新定义了一个变量，因此在递归中传入指针变量，则函数内部可以改变该指针指向内存内容，无法改变指针指向，故需传入引用或者定义为返回指针






    	
